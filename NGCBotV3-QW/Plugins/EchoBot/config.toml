# 插件基本信息
name = "回声插件"
version = "1.0.0"
author = "云山"
description = "回声机器人，自动回复收到的文本消息"

# 是否启用插件
enabled = false

# 插件自定义配置
[settings]
# 是否启用调试模式
debug = true

# 回复前缀
replyPrefix = "[EchoBot] "

# 是否只在群聊中工作
onlyInGroup = false

